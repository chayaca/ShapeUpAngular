export class Node {
    tableId:number;
    checked:boolean=false;
    // table:tables;
     shape: {type: string, shape: string, cornerRadius: 0};
    offsetX: number;
    width: number;
    height: number;
    offsetY: number;
}
